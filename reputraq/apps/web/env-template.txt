# Copy to .env.local and fill in your values.
# For Vercel: add the same variables in Project Settings → Environment Variables.

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# Drizzle / PostgreSQL (use Supabase Transaction pooler, port 6543)
DATABASE_URL=postgres://postgres.PROJECT_REF:PASSWORD@aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require

# Ensemble Social API
NEXT_PUBLIC_ENSEMBLE_API_URL=https://ensembledata.com/apis
ENSEMBLE_TOKEN=your_ensemble_token

# Apitube (news / competitor data)
APITUBE_KEY=your_apitube_key

# Dashboard AI chatbot (required for chatbot to work)
GEMINI_API_KEY=your_gemini_api_key

# Optional: set after deploy for frontend API base URL
# NEXT_PUBLIC_BACKEND_URL=https://your-app.vercel.app
# BASE_URL=https://your-app.vercel.app
